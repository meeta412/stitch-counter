{
  "installCommand": "cd frontend && npm install --userconfig=.npmrc --registry=https://registry.npmjs.org/",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist"
}
